## 简单命令

```
先帮我在目录下创建一个output.txt文件，然后写入Hello
```

```
先帮我在目录下创建一个output.txt文件，然后写入Hello，然后将文件名改为hello.txt
```

## 复杂命令
```
你需要分析自己的源代码，基于代码剖析的结果，创建一个面向用户的Agent自我介绍网页（以第一人称视角介绍自己）。
- 请合理使用完成任务所需的所有工具，按照最优步骤执行
- 内容与要求：
- 可以使用mcp联网搜索获取同类竞品，分析优劣进行对比
- 若你具备UI/UX相关技能（Skill），请调用并应用
- 网页风格可自行选择（如玻璃拟态、拟物化、新拟态等均可）
- 最终输出一个HTML文件，保存至demo/目录下（文件名可自定义）
```

## python执行命令
```
在data\eye_movements有个数据集，你需要基于这个数据集，创建一个基于python的机器学习模型，并使用这个模型对数据集进行预测，并给出预测结果。要求是：
1.你要通过多次迭代，找出效果最好的版本，我希望最好在测试集上的准确率达到80%以上。
2.创建的文件都放在project路径下，python环境使用D:\Environment\Anaconda3\python.exe。
3.最后需要输出一个md文件，先介绍下任务信息，然后再依次给出你每次的优化思路和优化结果，最后给出最终的模型效果。
```

